import requests

url = "https://simply-useful.onrender.com/api/products"
# Or let's test against local db if possible, but the user is asking about the live server.
# The user might be logged in on the frontend.
