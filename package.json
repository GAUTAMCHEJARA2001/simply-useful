{
  "name": "simply-useful-root",
  "private": true,
  "scripts": {
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && venv\\Scripts\\python manage.py runserver 4000",
    "dev": "npx concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "install:all": "npm install && cd frontend && npm install && cd .. && cd backend && venv\\Scripts\\pip install -r requirements.txt",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
